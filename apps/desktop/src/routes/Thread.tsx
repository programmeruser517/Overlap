import React from "react";

export function Thread(): React.ReactElement {
  return (
    <div style={{ padding: "2rem" }}>
      <h2>Thread</h2>
      <p>Thread view — mirror of web app/thread/[id].</p>
    </div>
  );
}
