import React from "react";

export function Settings(): React.ReactElement {
  return (
    <div style={{ padding: "2rem" }}>
      <h2>Settings</h2>
      <p>Calendar and email connections — desktop-only: tray, native dialogs.</p>
    </div>
  );
}
